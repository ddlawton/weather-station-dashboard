# name: R-CI

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   test:
#     runs-on: ubuntu-latest

#     services:
#       postgres:
#         image: postgres:15
#         env:
#           POSTGRES_DB: weatherdata
#           POSTGRES_USER: dlawton
#           POSTGRES_PASSWORD: testpassword
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd="pg_isready -U dlawton -d weatherdata"
#           --health-interval=10s
#           --health-timeout=5s
#           --health-retries=5

#     env:
#       DB_HOST: localhost
#       DB_PORT: 5432
#       DB_NAME: weatherdata
#       DB_USER: dlawton
#       DB_PASSWORD: testpassword
#       R_COMPILE_AND_INSTALL_PACKAGES: never

#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up R
#         uses: r-lib/actions/setup-r@v2
#         with:
#           r-version: '4.4.0'

#       - name: Install system dependencies
#         run: sudo apt-get update && sudo apt-get install -y libpq-dev libcurl4-openssl-dev

#       - name: Restore R package library
#         run: Rscript -e 'install.packages("renv"); renv::restore()'

#       - name: Wait for PostgreSQL
#         run: |
#           for i in {1..30}; do
#             pg_isready -h localhost -U dlawton -d weatherdata && break
#             sleep 2
#           done

#       - name: Seed test database
#         run: |
#           PGPASSWORD=$DB_PASSWORD psql -h localhost -U $DB_USER -d $DB_NAME -f tests/seed.sql

#       - name: Run tests
#         run: Rscript -e 'testthat::test_dir("tests")'
